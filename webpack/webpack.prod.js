// Below const and defined plugins section can be used to define own environment variables
// const webpack = require('webpack');
module.exports = {
  mode: 'production',
  devtool: 'source-map',
  //   plugins: [
  //     new webpack.DefinePlugin({
  //         'process.env.name': JSON.stringify('Prod Environment')
  //     })
  //   ]
};
